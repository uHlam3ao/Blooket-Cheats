(async () => {
    let n = document.createElement("iframe");
    n.style.display = "none";
    document.body.append(n);
    
    const foodAmount = n.contentWindow.prompt("Enter Food Amount (e.g. 999999):");
    
    if (foodAmount) {
        const getReact = (el) => {
            for (const key in el) {
                if (key.startsWith("__reactInternalInstance$") || key.startsWith("__reactFiber$")) 
                    return el[key];
            }
        };

        try {
            let stateNode = getReact(document.querySelector("#app > div > div")).return.stateNode;
            
                stateNode.setState({ food: Number(foodAmount) });
            
            n.contentWindow.alert("Food set to " + foodAmount + "!");
        } catch (e) {
            n.contentWindow.alert("Error: Make sure you are on the Café game screen.");
        }
    }
    
    n.remove();
    console.clear();
})();
