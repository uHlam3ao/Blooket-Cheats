(() => {
    let n = document.createElement('iframe');
    n.style.display = 'none';
    document.body.append(n);
    
    const matAmount = n.contentWindow.prompt('Enter Crazy Kingdom Materials amount (0-100):');
    
    if (matAmount && !isNaN(matAmount)) {
        const getReact = (el) => {
            for (const key in el) {
                if (key.startsWith('__reactInternalInstance$') || key.startsWith('__reactFiber$')) {
                    return el[key];
                }
            }
        };

        try {
            let reactElem = getReact(document.querySelector('#app > div > div'));
            if (reactElem && reactElem.return && reactElem.return.stateNode) {
                let stateNode = reactElem.return.stateNode;
                
                stateNode.setState({ 
                    materials: Number(matAmount)
                });
                
                n.contentWindow.alert(`Materials set to ${matAmount}!`);
            } else {
                n.contentWindow.alert('Game instance not found!');
            }
        } catch (e) {
            n.contentWindow.alert('Error updating materials!');
        }
    }
    
    n.remove();
    console.clear();
})();
